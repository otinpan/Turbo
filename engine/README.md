# Turbo Engine TODO

## P1 - レンダラーの基本機能

- [ ] Transformを`position`、`rotation`、`scale`から作り、各`RenderObject`が個別に動けるようにする
- [ ] カメラを実装し、view/projection行列をカメラ状態から更新する
- [ ] キーボードとマウスでカメラを操作できるようにする
- [ ] フレーム時間`delta_time`を導入し、時間依存の移動や回転を安定させる
- [ ] 三角形描画ヘルパーを用意する
- [ ] 四角形描画ヘルパーを用意する
- [ ] 複数ポリゴンや任意メッシュを生成して描画できるAPIを用意する
- [ ] 立方体、平面、球などの基本プリミティブを生成できるようにする
- [ ] 複数メッシュ、複数テクスチャ、複数マテリアルを扱えるようにする
- [ ] OBJ以外のモデル形式を検討し、必要ならglTF読み込みへ進む
- [ ] テクスチャをモデルごと、マテリアルごとに切り替えられるようにする
- [ ] descriptor setをオブジェクト、マテリアル、フレーム単位で整理する
- [ ] push constantとuniform bufferの役割を整理する
- [ ] swapchain再作成時に関連リソースが漏れなく再作成、破棄されるようにする
- [ ] depth、MSAA、framebuffer、pipelineの再作成単位を関数として整理する
- [ ] shaderのコンパイル手順を自動化し、失敗時に分かりやすいエラーを出す

## P2 - ゲームループとアプリ構造

- [ ] 入力、更新、描画を分けたゲームループにする
- [ ] `App`、`World`、`Renderer`、`Input`、`Time`の責務を分ける
- [ ] ウィンドウイベント処理をアプリ本体から切り出す
- [ ] シーンの読み込み、更新、破棄のライフサイクルを作る
- [ ] Entityを導入し、ゲーム内オブジェクトをIDで管理する
- [ ] Transform、MeshRenderer、Cameraなどのコンポーネント構造を検討する
- [ ] シーン内のオブジェクト追加、削除、検索を安全に行えるようにする
- [ ] 固定更新と可変更新の扱いを決める
- [ ] ポーズ、ステップ実行、リセットを実装する
- [ ] FPS、フレーム時間、描画オブジェクト数を表示またはログ出力する
- [ ] 設定ファイルからウィンドウサイズ、VSync、MSAA、アセットパスを読み込む

## P3 - アセット管理

- [ ] アセットディレクトリ構成を決める
- [ ] モデル、テクスチャ、シェーダーをアセットIDで参照できるようにする
- [ ] 同じアセットを二重ロードしないキャッシュを作る
- [ ] 読み込み失敗時に代替アセットや分かりやすいエラーを出す
- [ ] 画像読み込みをPNG固定から拡張し、必要ならJPEGやTGAにも対応する
- [ ] mipmap生成の有無をアセット単位で選べるようにする
- [ ] sampler設定をテクスチャごとに持てるようにする
- [ ] モデルの頂点属性、法線、UV、接線を扱えるようにする
- [ ] アセットのホットリロードを検討する
- [ ] 将来的なパッケージ化や配布用アセットバンドルを検討する

## P4 - 描画品質

- [ ] PhongまたはBlinn-Phongの基本ライティングを実装する
- [ ] directional light、point light、spot lightを扱えるようにする
- [ ] 法線を使った陰影計算を追加する
- [ ] ガンマ補正とsRGBテクスチャの扱いを整理する
- [ ] アルファブレンドを実装し、透明オブジェクトを描画できるようにする
- [ ] 不透明パスと透明パスを分ける
- [ ] face culling、front face、depth test、depth writeの設定を整理する
- [ ] ワイヤーフレーム表示や法線表示などのデバッグ描画を追加する
- [ ] skyboxまたは背景描画を追加する
- [ ] シャドウマップを実装する
- [ ] PBRマテリアルを検討する
- [ ] bloom、tone mapping、color gradingなどのポストプロセスを検討する
- [ ] render passを複数パス構成に拡張できるようにする

## P5 - パフォーマンス

- [ ] フレームごとのメモリアロケーションを減らす
- [ ] command bufferの再記録範囲を必要最小限にする
- [ ] secondary command bufferの使い方を計測し、効果がある形に整理する
- [ ] オブジェクト数が増えても破綻しないdescriptor管理にする
- [ ] uniform bufferをdynamic uniform bufferやstorage bufferへ拡張するか検討する
- [ ] meshごとの描画をソートし、pipelineやdescriptorの切り替えを減らす
- [ ] frustum cullingを実装する
- [ ] バウンディングボックスやバウンディングスフィアを持たせる
- [ ] インスタンシングで同一メッシュを大量描画できるようにする
- [ ] GPUメモリ使用量とバッファ数をログで確認できるようにする
- [ ] プロファイリング用のCPU/GPUタイマーを追加する

## P6 - 入力と操作

- [ ] キーボード入力を押下、押している間、離した瞬間で扱えるようにする
- [ ] マウス移動、クリック、ホイール入力を扱えるようにする
- [ ] ゲームパッド対応を検討する
- [ ] 入力マッピングを作り、キー設定を変更できるようにする
- [ ] カメラ操作、オブジェクト選択、デバッグ操作を入力システム経由にする

## P7 - 物理とゲーム機能

- [ ] 2Dまたは3Dの当たり判定方式を決める
- [ ] AABB、球、レイの交差判定を実装する
- [ ] レイキャストでオブジェクト選択できるようにする
- [ ] 重力、速度、加速度の簡単な物理更新を追加する
- [ ] 既存の物理ライブラリ導入を検討する
- [ ] ゲーム状態、スコア、HP、イベントなどの基本システムを用意する
- [ ] シーン遷移を実装する
- [ ] セーブ、ロードの形式を検討する

## P8 - ツールと開発体験

- [ ] 実行、shaderコンパイル、format、checkをまとめるスクリプトを用意する
- [ ] READMEにビルド方法、実行方法、必要なVulkan SDKを記載する
- [ ] エディタ設定や推奨拡張を整理する
- [ ] CIで`cargo fmt`、`cargo clippy`、`cargo test`を走らせる
- [ ] サンプルシーンを用意する
- [ ] エンジン機能を確認する小さなデモを増やす
- [ ] デバッグUIの導入を検討する
- [ ] シーン、アセット、マテリアルを編集する簡易エディタを検討する

## P9 - テストと品質保証

- [ ] 行列計算、Transform、カメラの単体テストを追加する
- [ ] アセット読み込みのテストを追加する
- [ ] メッシュ生成のテストを追加する
- [ ] swapchainなしでテストできる純粋ロジックを分離する
- [ ] Vulkan依存部分は手動検証手順をREADMEに残す
- [ ] ウィンドウリサイズ、最小化、復帰、終了の確認項目を作る
- [ ] validation layerのログ確認をリリース前チェックに入れる
- [ ] メモリリークやリソース破棄漏れを確認する手順を作る

## P10 - 配布と将来拡張

- [ ] debug/releaseビルドの違いを確認する
- [ ] リリースビルドでアセットを同梱して実行できるようにする
- [ ] Windows以外の対応可否を整理する
- [ ] renderer backendを差し替えられる設計を検討する
- [ ] マルチスレッド化できる部分を洗い出す
- [ ] ジョブシステムを検討する
- [ ] オーディオシステムを検討する
- [ ] UI描画を検討する
- [ ] スクリプト機能を検討する
- [ ] ネットワーク機能を検討する
